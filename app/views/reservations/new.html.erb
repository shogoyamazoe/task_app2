<h1>予約内容確認</h1>

<div class="container mt-5">
  <div class="row">
    <div class="col-30 mx-auto">
    <h5>この内容で予約しますがよろしいですか？</h5>
      <table class="table table-hover">
        <thead class="thead-dark">
          <tr>
            <th scope="col">予約開始日</th>
            <th scope="col">予約終了日</th>
            <th scope="col">予約人数</th>
            <th scope="col">使用日数</th>
            <th scope="col">合計金額</th>
            
          </tr>
          </thead>
          <tbody>
          <tr>
          <td><%= @reservation.start_day %></td>
          <td><%= @reservation.end_day%></td>
          <td><%= @reservation.customer%>人</td>
          <td><%= @reservation.total_days%>日</td>        
          <td>¥<%= @reservation.total_price %></td>
          </tr>
          </div>
          </div>
      </table>
          <%= form_with model:@reservation,method: :post ,local: true do |reservar| %>
          <p><%= reservar.submit "登録完了",class: "btn btn-primary"%></p>
     
          
              <%= reservar.hidden_field :start_day,value: @reservation.start_day %>
              <%= reservar.hidden_field :end_day,value: @reservation.end_day %>
              <%= reservar.hidden_field :customer,value: @reservation.customer %>
              <%= reservar.hidden_field :total_days,value: @reservation.total_days %>
              <%= reservar.hidden_field :total_price,value: @reservation.total_price %>
              <%= reservar.hidden_field :room_id,value: @reservation.room_id %>
          <% end %>

          <%= form_with model: @reservation, url:back_reservations_path, method: :get ,local: true do |reservar| %>
          <p><%= reservar.submit "詳細へ戻る",class: "btn btn-primary"%></p>
         
          
              <%= reservar.hidden_field :start_day,value: @reservation.start_day %>
              <%= reservar.hidden_field :end_day,value: @reservation.end_day %>
              <%= reservar.hidden_field :customer,value: @reservation.customer %>
              <%= reservar.hidden_field :total_days,value: @reservation.total_days %>
              <%= reservar.hidden_field :total_price,value: @reservation.total_price %>
              <%= reservar.hidden_field :room_id,value: @reservation.room_id %>
             
      <% end %>
    
      <p><%= link_to "ホームへ戻る", root_path,class: "btn btn-primary"%></p>